```@meta
CollapsedDocStrings = true
CurrentModule = TensorKitTensors.QuantumGates
```

# Quantum gates

Common single-, two- and three-qubit gates used in quantum circuits, returned as `TensorMap`s on qubit spaces.

## Conventions

### Basis ordering

A qubit lives on a two-dimensional space with computational basis ordered as ``\{|0⟩, |1⟩\}``.

Multi-qubit gates act on ``V^{⊗n}`` with the first qubit as the leftmost tensor factor, so a controlled gate uses the *first* qubit(s) as control and the *last* as target.

Within a fused block, the index of ``|b_1 b_2 \ldots b_n⟩`` is ``k = 1 + \sum_i b_i 2^{i-1}``, i.e. the *first* qubit runs fastest.
Consequently `block(gate, Trivial())` agrees element-for-element with the gate matrices as tabulated by e.g. Qiskit, identifying the first tensor factor with qubit 0.

### Element types

The gates with complex matrix elements — [`pauli_y`](@ref), [`phase_shift`](@ref), [`s_gate`](@ref), [`t_gate`](@ref), [`rotation_x`](@ref), [`rotation_z`](@ref), [`u3`](@ref), [`cy`](@ref), [`cs`](@ref), [`cphase`](@ref), [`iswap`](@ref), [`ecr`](@ref), [`rotation_xx`](@ref), [`rotation_yy`](@ref), [`rotation_zz`](@ref) and [`rotation_zx`](@ref) — throw an `ArgumentError` when requested with a real `eltype`.
All others are real-valued and honour any `eltype`, including exact ones such as `Int`.

### Rotation angles

The parametrized gates take their rotation angle as a required `theta` keyword, e.g. `rotation_x(; theta = π / 2)`.
The universal single-qubit gate [`u3`](@ref) takes the three angles `theta`, `phi` and `lambda`.

### Symmetry sectors

| Symmetry | Physical meaning | Sector label | Representable gates |
|---|---|---|---|
| `Trivial` | none | — | all gates, full ``2^n \times 2^n`` matrix |
| `Z2Irrep` | excitation-number parity | charge ``n \bmod 2`` per qubit | only gates that conserve the number of ``1``'s modulo 2 |
| `U1Irrep` | excitation-number conservation | charge ``∈ \{0, 1\}`` per qubit | only gates that conserve the number of ``1``'s |

!!! note "U(1) charge = excitation number"
    Under `U1Irrep` the two sectors of a qubit carry charge ``0`` (``|0⟩``) and ``1`` (``|1⟩``), i.e. the conserved quantity is the number of excited qubits.
    Only number-conserving gates are representable in this symmetry: [`pauli_z`](@ref), [`proj_0`](@ref), [`proj_1`](@ref), [`s_gate`](@ref), [`t_gate`](@ref), [`phase_shift`](@ref), [`rotation_z`](@ref), [`cz`](@ref), [`cs`](@ref), [`cphase`](@ref), [`swap`](@ref), [`iswap`](@ref), [`rotation_zz`](@ref) and [`fredkin`](@ref).
    Gates that flip qubits (X, Y, H, CNOT, Toffoli, …) throw an `ArgumentError` when requested with `U1Irrep`.

!!! note "Z₂ charge = excitation parity"
    The ``ℤ₂`` symmetry implemented here is the parity of the excitation number, which is **diagonal** in the computational basis — unlike the ``ℤ₂`` of [`SpinOperators`](spinoperators.md), which is the spin flip generated by ``e^{iπS^x}`` and requires a change of basis.
    It is a weaker requirement than `U1Irrep`, so everything in the list above remains representable, and in addition [`rotation_xx`](@ref) and [`rotation_yy`](@ref) become available: ``X ⊗ X`` and ``Y ⊗ Y`` flip both qubits and hence preserve the total parity.

## Gate combinators

[`controlled`](@ref) turns any gate into its controlled version ``|0⟩⟨0| ⊗ I + |1⟩⟨1| ⊗ G``, inheriting the element type and the symmetry of `G`, and stacking arbitrarily: `controlled(controlled(pauli_x()))` is the [`toffoli`](@ref) gate.
Together with the adjoint `G'` and the integer powers `G^n` of TensorKit, this covers the usual gate modifiers — inverse, power and control — where a gate controlled on ``|0⟩`` follows from swapping the two projectors.
The gates that are not provided in controlled form, such as the controlled rotations, are therefore one-liners:

```julia
crx = controlled(rotation_x(; theta = π / 2))
```

## Gate overview

| Function | Alias(es) | Sites | Supported symmetries |
|---|---|---|---|
| [`qubit_space`](@ref) | — | — | `Trivial`, `Z2Irrep`, `U1Irrep` |
| [`pauli_x`](@ref) | `X` | 1 | `Trivial` |
| [`pauli_y`](@ref) | `Y` | 1 | `Trivial` |
| [`pauli_z`](@ref) | `Z` | 1 | `Trivial`, `Z2Irrep`, `U1Irrep` |
| [`proj_0`](@ref) | `P0` | 1 | `Trivial`, `Z2Irrep`, `U1Irrep` |
| [`proj_1`](@ref) | `P1` | 1 | `Trivial`, `Z2Irrep`, `U1Irrep` |
| [`hadamard`](@ref) | `H` | 1 | `Trivial` |
| [`s_gate`](@ref) | `S` | 1 | `Trivial`, `Z2Irrep`, `U1Irrep` |
| [`t_gate`](@ref) | `T` | 1 | `Trivial`, `Z2Irrep`, `U1Irrep` |
| [`phase_shift`](@ref) | `P` | 1 | `Trivial`, `Z2Irrep`, `U1Irrep` |
| [`rotation_x`](@ref) | `Rx` | 1 | `Trivial` |
| [`rotation_y`](@ref) | `Ry` | 1 | `Trivial` |
| [`rotation_z`](@ref) | `Rz` | 1 | `Trivial`, `Z2Irrep`, `U1Irrep` |
| [`u3`](@ref) | `U` | 1 | `Trivial` |
| [`cnot`](@ref) | `CNOT`, `CX` | 2 | `Trivial` |
| [`cy`](@ref) | `CY` | 2 | `Trivial` |
| [`cz`](@ref) | `CZ` | 2 | `Trivial`, `Z2Irrep`, `U1Irrep` |
| [`ch`](@ref) | `CH` | 2 | `Trivial` |
| [`cs`](@ref) | `CS` | 2 | `Trivial`, `Z2Irrep`, `U1Irrep` |
| [`cphase`](@ref) | `CP` | 2 | `Trivial`, `Z2Irrep`, `U1Irrep` |
| [`swap`](@ref) | `SWAP` | 2 | `Trivial`, `Z2Irrep`, `U1Irrep` |
| [`iswap`](@ref) | `ISWAP` | 2 | `Trivial`, `Z2Irrep`, `U1Irrep` |
| [`dcx`](@ref) | `DCX` | 2 | `Trivial` |
| [`ecr`](@ref) | `ECR` | 2 | `Trivial` |
| [`rotation_xx`](@ref) | `Rxx` | 2 | `Trivial`, `Z2Irrep` |
| [`rotation_yy`](@ref) | `Ryy` | 2 | `Trivial`, `Z2Irrep` |
| [`rotation_zz`](@ref) | `Rzz` | 2 | `Trivial`, `Z2Irrep`, `U1Irrep` |
| [`rotation_zx`](@ref) | `Rzx` | 2 | `Trivial` |
| [`toffoli`](@ref) | `TOFFOLI`, `CCX` | 3 | `Trivial` |
| [`fredkin`](@ref) | `FREDKIN`, `CSWAP` | 3 | `Trivial`, `Z2Irrep`, `U1Irrep` |

The adjoint gates ``S^\dagger`` and ``T^\dagger`` are obtained as `s_gate()'` and `t_gate()'`.

## API

```@autodocs
Modules = [QuantumGates]
```
